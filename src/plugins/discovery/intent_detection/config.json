{
  "schema": 1,
  "description": "Detect the user's intent from their query",
  "execution_settings": {
    "default": {
      "max_tokens": 50,
      "temperature": 0.0,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "user_query",
      "description": "The user's query to analyze for intent",
      "default": "",
      "is_required": true
    }
  ]
}

